# Workers API URL（ローカル開発時は wrangler dev のアドレス）
VITE_WORKERS_URL=http://localhost:8787

# Workers admin token（wrangler.toml の [vars] ADMIN_TOKEN と一致させる）
VITE_ADMIN_TOKEN=dev-admin-token #本番でハッシュ値をgithub secretsに入れる
